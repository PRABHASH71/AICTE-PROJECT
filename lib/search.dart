class search {
  final String title;
  const search({
    required this.title,
  });
}

const allSearch = [
  search(title: 'About'),
  search(title: 'Approval'),
  search(title: 'Meetings'),
  search(title: 'Events'),
  search(title: 'Fellowship'),
  search(title: 'Schemes'),
  search(title: 'Help'),
  search(title: 'Emaildesk'),
];
